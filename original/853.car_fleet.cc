#include "common.h"
//car fleet 就是车并排的意思， 在到达 target 之前（含），有多少组 car fleet
int solve(vector<int> positions, vector<int> speed, int target) {
    int res = 0;
    int n = positions.size();

    return res;
}


int main() {


}
